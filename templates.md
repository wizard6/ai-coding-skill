# AI Coding 模板速查（v1.35）

## 索引文件模板

### INDEX.md

```markdown
# 项目索引

## 模块总览
| 模块 | 职责 | 入口文件 | 依赖模块 |
|------|------|---------|---------|
| user | 用户注册/登录 | src/user/index.ts | auth, db |

## 关键位置
- 组合根: src/index.ts
- 索引: INDEX.md, .ai-context
```

### .ai-context（≤50 行）

```text
tech: <stack>
layout: src/<domain>/… ; root=src/index.ts
layers: gateway→adapters→features/core (deps inward; DTO across)
conventions: file≤300-default (cohesion>lines; exceed+1-reason; no degrade-to-fit); fn≤30; nest≤3; hotspots→config/list/file; stable→merge; boy-scout-in-confirm; confirm-first
prefs: <长期偏好，如 no-native-scrollbar; theme-contrast-pairs>
commands: <test> / <lint> / <dev>
indexes: INDEX.md, API-INDEX.md, DB-INDEX.md, docs/EFFECTS.lock.md, docs/FEATURE.lock.md
```

### docs/EFFECTS.lock.md（效果锁）

```markdown
# 效果锁

已验收观感/交互；未确认不得改下列条目。

- [锁] 星云气质 — 深色墨底 + 夏威夷色温；禁改成亮色仪表盘风 — src/gateway/web/theme.css
- [锁] … — … — …
```

### docs/FEATURE.lock.md（功能锁）

```markdown
# 功能锁

已验收行为/契约；未确认不得改下列条目。

- [锁] 后台鉴权 — /api/admin 须 ADMIN_TOKEN；生产未配置则拒绝 — server/src/auth.ts
- [锁] 恒星语义 — 一作品一星；版本不拆星 — README 概念约定
- [锁] … — … — …
```

### 文件头

```typescript
// 文件 / 职责 / 导出 / 依赖（无框架类型）
// === 区 ===
```

---

## 协作提示词

```text
【诊断】对本切片做最小架构诊断；只输出 3～7 条 list；不动手。
【确认】拟改：目标一句、文件列表、不改范围、可选童子军/顺带项；等人确认。缺口可靠推不出→模式1直接问（≤3）；上下文已够→模式2推出 list（推断标「推断」），不问可推之事。
【设计】需求：[一句]。按 INDEX 用表给设计，≤50 行。
【编码】按确认 list 改 [path]；只 diff + 行号。
【修改】文件：[path]；[原→目标]；diff；范围外不改。
【测试】为 [path] 的 [函数] 写测试；只给测试代码。
【定位】[功能] → INDEX → 5 句内指到文件。
【快诊】大范围打分 X/10（k/7）+ 失败项 + 反转（见 reference 3.B）。
【热点】常改点 → 3.10 方案表。
【归位】碎片 → 3.11 目标或 TODO。
【债检】本切片新债表。
【根因】错/多/少改：核心原因 1 句 + 规避；是否写入 .ai-context。
【质疑】站在用户角度：表面建议是 X；用户真正要得到的是 Y。不清点：[≤3 问]。若 Y≠X：拒绝 X，给替代 1…2…；不动手。禁止用程序/架构目的替换 Y。
【技能体检】对 ai-coding 跑防腐烂表；只表+必改 diff。
【纪律回复】按 7.29 输出：结论前置、直接事实折叠、越权分级、不盲信；不动手改业务。
【会话SSOT】检查 memory：SSOT / 日志 / 经验沉淀 / 拆分外链；无 SSOT 则猜/问→确认；完成追加日志；可复用进经验；禁改 SSOT。
【自我优化】期望效果：[一句]。写不出则停止。然后：诊断 → 确认 → 落地。
【复盘】结果：[一句]。必要路径：[1–3 步]。可删/可脚本化：[项]。量级：小事/中等/大事；工具约 N 次；超预算：是/否。若提前知道 [X] 可少 [Y] 次。建议落点：项目 / 全局 / 不写。
【省耗】量级：小事/中等/大事。缺：[文件|位置|索引|其它]。动作：问用户 / 读 INDEX|README|打开文件 / 停搜。
【索引】发现可记：[巨石|入口|热点|踩坑] → 拟写一行：[…]。是否写入 INDEX？提醒：索引可能过期。未确认不改。
【提交】脏树或中等/大事前：引导语「请回复编号选择」+ text 围栏内分行 ○1 先提交 / ○2 不提交 / ○其他（对用户须真换行）。
【锁】已读 EFFECTS/FEATURE.lock。触锁：[条目]。缺文件则问是否创建。
【薄宏观】目的：[用户得到什么]。边界：[权限/租户/副作用/不改]。契约：[DTO|Tool|事件]。验收：[可操作一步]。锁：[触 7.24？]。半页内；不动手。
【聚焦】一句话重定义：[…]。In：[…]。Out：[≥2]。假设风险：[…]。主链+Plan B：[…]。输出规格：[…]。三检：结论/科普/行动。不动手。
【层级】判定 L1|L2|L3|L4：[依据一句]。默认行为：[澄清|结构|确认设计|实现]。未授权不落码。
【清单总结】已做：[…]。未做：[无|…]。下一步：[1–3]。
【UI检查】按 7.15+7.17 实测；口头过检无效。
【外取参考】触发 7.16：停盲猜；按类给外站；等用户带回参考。
【NAV规则】点名 rules 时先拉目录再动手。
【安装技能】复制 NAV「安装方法」粘贴块；或把 `~/.cursor/skills/ai-coding/` 放到本机 skills 目录。
```

### 确认 list 示例

```text
1. 目标：把笔记页滚动条换成自定义样式。
2. 文件：notes/css/notes.css、shared/css/scrollbar.css（新建）。
3. 不改：其它页面、滚动逻辑、JS。
4. 可选童子军：抽 shared 滚动条变量（需你点头）。
```

中等/大事须加薄宏观块（7.25）：

```text
【薄宏观】
- 目的：用户能在门户用机器人查制度并看到引用。
- 边界：只读知识库；无发邮件；tenant 过滤必做。
- 契约：retrieve_policy Tool Schema；citation 字段进响应。
- 验收：无依据时拒答；黄金集 20 条引用可追溯。
- 锁：拟写入 FEATURE.lock「制度问答须引用」——是否创建？
```

需要选方案时（**选项进 \`\`\`text 围栏，每项一行**）：

请回复编号选择（可多选如 1+3）：

```text
○ 1 — 拆 spatialView3d.ts（星云主场景）
○ 2 — 拆 chrome.ts（书卡面板）
○ 其他 — 请直接说明你的选项
```

### 7.20 复盘示例

```text
结果：后台已有 ADMIN_TOKEN 鉴权，ops:start 可一键起服。
必要路径：写 auth 中间件 → 挂 /api/admin → 后台带 token → AI-OPS 三条命令。
可删：中途多次手工探端口；应直接跑 ops 脚本。
落点建议：项目 → docs/AI-OPS.md（已有）；全局 → 不写（已是 7.19）。
请确认是否另加 .cursor/rules/ops.mdc？
```

### 7.21 省耗示例

```text
量级：小事（改顶栏品牌色）
缺：未点名文件。
动作：问「是否改 src/gateway/web/styles.css 的 --brand？或当前打开文件？」——不先 grep 全仓。
若常改：建议抽 styles 顶部 :root tokens，INDEX 加一行「改外观看 styles.css :root」。
```

### 7.22 索引增补示例

```text
发现：spatialView3d.ts ≈860 行，星云主场景（巨石）。
拟记 INDEX：「星云渲染 → spatialView3d.ts」。
提醒：索引可能过期。
请回复编号选择：后接 text 围栏，内含分行：
○ 1 — 写入 INDEX
○ 2 — 暂时不记
○ 其他 — 请直接说明
```

### 7.18 取证示例

```text
# 模式1（缺信息）
还不清楚要改桌面还是移动端顶栏。问：1）改哪一端？2）是否动交互逻辑？

# 模式2（上下文已够）
你已说「只改 notes 页滚动条、不动 JS」。推断 list 如下（推断已标）：
1. 目标：…（来自你本轮原话）
2. 文件：notes/css/notes.css（推断：仅样式）
3. 不改：JS、其它页
请确认后动手。
```

### 根因表示例

```text
| 类型 | 核心原因 | 规避 |
|------|----------|------|
| 少改 | 改背景色时未联动字色 token | 主题色成对变量；改 bg 必查 text |
| 错改 | 默认用了浏览器原生滚动条 | .ai-context prefs: no-native-scrollbar |
| 误判 | 用户没说参考，却把兄弟项目当作风参照 | 守 7.14：没说参考就不参考；仅强相关例外；扫目录只避撞名/不改 |
| 多改 | 用户说不清风格仍连续猜观感 | 守 7.16：停盲猜；按类外取；等用户带回参考 |
```

### 终点输出示例（7.11 + 7.13）

```markdown
## 结论
你现在可以决定要不要改笔记页滚动条。我建议先改这一页；全站统一先放着，等你点头再做。

## 分条
1. **你要得到的结果** — 笔记页不再用浏览器默认滚动条，改成你习惯的自定义样式。确认后我改样式文件。
2. **会动哪些文件** — 主要改 `notes/css/notes.css`；若你要全站复用，再新建 `shared/css/scrollbar.css`。
3. **我不会动什么** — 不改 JavaScript，也不改滚动逻辑，避免顺带弄坏行为。
4. **可选** — 全站统一自定义滚动条。默认不做；你说「按第 4 条执行」我再做。
```

---

### 7.30 会话 memory

```markdown
## [SSOT] · 唯一真相（确认后禁止改写；只写目标+环境）
- **核心目标**：…
- **当前环境**：…
- **确认**：YYYY-MM-DD …

## 经验沉淀（方案 / 常见问题 / 配置操作；可外链）
- [方案] … — 确认要点 …
- [常见问题] … — 根因一句 — 处理/规避 …
- [配置操作] … — 动了什么（路径/键）— 为何 …

## 日志（只追加「做了什么」；勿改 SSOT）
- [完成] YYYY-MM-DD：…
- [问] YYYY-MM-DD：…

## 索引（可选）
- 话题拆分：`memory/topics/xxx.md`
- 按月：`memory/2026-08.md`
- 外链：…
```

无 SSOT 时引导语示例：「请回复编号选择：」+ text 围栏：
`○ 1 — 按推断写入 SSOT 并继续` /
`○ 2 — 我重写核心目标/环境` /
`○ 其他 — 请直接说明`

---

### 7.29 纪律回复骨架

```markdown
**结论 / 答：** …

<details>
<summary>直接事实</summary>

- 在哪：path + 行号
- 原文 / 改前→改后：关键几行
- 一句白话：这几行在什么条件下做了哪一步

</details>

<details>
<summary>意图 · 下一步</summary>

- 表面：…
- 初衷：…（置信）
- 语言层级：语言 Lx
- 冲突：未见 / …
- 预测下一步：…（还没做）

</details>
```

---

## Token 速查

| 行为 | 优化 |
|------|------|
| 贴全文 | 索引+行号 |
| 未确认就改 | 先【确认】list |
| 偷扩 scope | 「不改」写清；童子军仅确认项 |
| 重复踩坑 | 【根因】→ .ai-context prefs |
| 为凑 300 行砍功能或假拆 | 守 1.5：效果与内聚优先；超限写一句理由 |
| 界面未过布局/留白/状态色检查 | 跑【UI检查】7.15；失败项当场修 |
| 原生滚动条/alert/蓝选区/默认光标当终态 | 守 7.17（NAV ui-surface） |
| 正式路径堆 TODO/假按钮 | 守 6.9；改演示页表达意图 |
| 观感不满或说不清风格 | 跑【外取参考】7.16；停盲猜；等用户带回图/仓库 |
| 没说参考却对齐兄弟 | 守 7.14；确认 list 禁「可对齐某某」除非明确参考或强相关 |
| skill 膨胀 | 【自我优化】须有期望效果；无效果不做 |
| 用户看不懂或看太慢 | 先结论与决策点；遵守 7.13 |
| 用比喻代替说明 | 对用户改直述 |
| 从程序角度偷换目的 | 回到用户要得到什么；遵守 7.12 |
| 盲从用户方案 | 先对齐用户目的；不合适就拒绝并给替代 |
| 一次写出全部详情 | 先写结论；详情进折叠（7.29）；用户点名后再展开 |
